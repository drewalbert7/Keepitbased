# Copy to .env.production before `npm run build` (file is gitignored).
# On app.keepitbased.com the Quant terminal auto-uses /api/quant-agi/sidecar (JWT).
# Override only for local/staging sidecar URLs, e.g. http://127.0.0.1:8844

# NEXT_PUBLIC_QUANT_AGI_URL=http://127.0.0.1:8844
