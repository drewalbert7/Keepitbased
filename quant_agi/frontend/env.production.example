# Copy to .env.production before `npm run build` (file is gitignored).
# In production behind nginx, the browser cannot call 127.0.0.1:8844 — use the public reverse-proxy path.

NEXT_PUBLIC_QUANT_AGI_URL=https://app.keepitbased.com/quant-sidecar
